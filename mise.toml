[tools]
wrangler = "4.65.0"

[tasks.build]
description = "Copy source files into public/"
run = "cp gear-calculator.html public/index.html"

[tasks.deploy]
description = "Build and deploy to Cloudflare Pages"
depends = ["build"]
run = "wrangler pages deploy public --project-name=bikegears --branch=main"
